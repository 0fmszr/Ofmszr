function fonksiyonAdi(parametreler) {
    // Fonksiyonun gerçekleştireceği işlemler
    return sonuc; // Sonuç döndürebilir
  }
  function carp(x, y) { // x ve y parametrelerdir
    return x * y;
  }
  
  console.log(carp(4, 5)); // 4 ve 5 argümanlardır
  carp(4, 5); // Fonksiyon burada çağrılıyor
  function fark(a, b) {
    return a - b; // Sonucu geri döndür
  }
  
  let sonuc = fark(10, 3); // sonuc değişkeni 7 olur
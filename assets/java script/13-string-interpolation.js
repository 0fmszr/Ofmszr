const fname ="john";
const lname ="doe";
const fullname = `${fname}  ${lname}`;
console.log(fullname)
console.log( `hello this is another way to write on js ${fname} ${lname} ! so ı can go like this too bb`)